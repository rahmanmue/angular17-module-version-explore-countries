import { CheckedValuePipe } from './checked-value.pipe';

describe('CheckedValuePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckedValuePipe();
    expect(pipe).toBeTruthy();
  });
});
