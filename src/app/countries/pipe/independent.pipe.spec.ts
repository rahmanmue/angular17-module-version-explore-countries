import { IndependentPipe } from './independent.pipe';

describe('IndependentPipe', () => {
  it('create an instance', () => {
    const pipe = new IndependentPipe();
    expect(pipe).toBeTruthy();
  });
});
